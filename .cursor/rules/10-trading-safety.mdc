---
description: 交易与资金相关的硬性安全约束
alwaysApply: true
---

# 交易安全护栏

## 绝不允许

- 让运行时 LLM 直接下单或生成订单动作。
- 未经明确人类批准接入/操作真实资金；默认一切 `TRADING_MODE=paper`。
- 把密钥/API key/凭证写入代码或提交到 git。只用 `.env`（见 `.env.example`）。

## 必须具备（涉及执行的代码）

- 单笔/总仓位限额、每日亏损熔断。
- 全局 kill switch（`KILL_SWITCH=true` 时禁止任何下单）。
- 异常/超时的安全降级（默认不交易，而非盲目下单）。
- 每笔交易全量留痕：触发信号、决策依据、执行结果。

## prompt 注入

LLM 读取的外部非结构化信息（新闻等）视为不可信输入，需隔离与校验，不得让其改变系统指令或触发越权动作。
